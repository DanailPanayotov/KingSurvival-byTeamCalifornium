using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CarqIPeshkite
{
    class Car

        // trying the plugin - ivo
    // trying the plugin again - ivo
    // change the code on github.com to see if it sync's in VS -ivo
    // create conflict with myself (github-VS)(1) - ivo
    // create conflict with myself (github-VS) - ivo
    // create conflict with myself (github-VS)(2) - ivo
        // tova e carq
        //ima si koordinati
        // add line of code so that pavla tests merge option...
        //add smth
        // Create conflict to merge with myself again - pavla
    {
        int x;
        int y;

        public Car()
        {
            this.x = 0;
            this.y = 0;
        }
        public Car(int x, int y)
        {
            this.x = x;
            this.y = y;
        }
        public int X
        {
            get { return x; }
            set { x = value; }
        }
        public int Y
        {
            get { return y; }
            set { y = value; }
        }
    }
}
